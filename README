 # 🚀 Système de Surveillance Intelligent : Votre Vue à 360° Sécurisée

---

## 🎯 Aperçu du Projet

Bienvenue sur le dépôt officiel du **Système de Surveillance Intelligent** ! Cette application web complète est conçue pour transformer la manière dont vous surveillez des zones spécifiques, gérez les alertes et obtenez des métriques en temps réel. Notre solution combine une **interface utilisateur intuitive** avec un **backend robuste et performant**, offrant une gestion de données fluide et sécurisée.

---

## ✨ Fonctionnalités Clés

Découvrez les capacités qui rendent notre système unique :

* **🔒 Authentification Robuste** : Accès sécurisé via une page de connexion dédiée, exigeant un nom d'utilisateur et un mot de passe.
* **🗺️ Tableau de Bord Interactif** : Visualisez vos zones surveillées en un coup d'œil grâce à une carte dynamique et interactive.
* **➕ Gestion Simplifiée des Zones** : Créez, modifiez et supprimez vos zones de surveillance en toute simplicité.
* **🚨 Alertes en Temps Réel** : Recevez des notifications instantanées et gérez efficacement les alertes critiques.
* **📈 Analyse & Métriques Détaillées** : Suivez l'activité du système et prenez des décisions éclairées grâce à des données clés et des visualisations graphiques.
* **📱 Interface Responsive Universelle** : Profitez d'une expérience utilisateur optimisée et fluide, quel que soit votre appareil (ordinateurs, tablettes, mobiles).

---

## 🏗️ Architecture Technique

Le projet est conçu avec une architecture client-serveur moderne, garantissant une séparation claire des responsabilités et une meilleure maintenabilité.

### 💻 Frontend : L'Expérience Utilisateur

Développé avec des technologies de pointe, le frontend assure une interaction fluide et une présentation dynamique des données.

* **Technologies phares** : **React**, **TypeScript**, HTML, CSS.
* **Structure des dossiers (extraits)** :
    * `client/src/App.tsx` : Le composant racine de l'application.
    * `client/src/pages/login.tsx` : La page d'authentification.
    * `client/src/pages/landing.tsx` : Le tableau de bord principal après connexion.
    * `client/src/hooks/useAuth.ts` : Un hook personnalisé pour une gestion d'authentification élégante.

### ⚙️ Backend : Le Cœur de l'Intelligence

Construit sur une base solide, le backend gère toute la logique métier, l'authentification des utilisateurs et l'accès aux données via une API RESTful.

* **Technologies phares** : **Flask (Python)**, Flask-CORS.
* **Fichier principal** :
    * `src/main.py` : Ce fichier contient le cœur du serveur Flask, définissant toutes les routes API et la logique d'authentification essentielle.

---

## 🚀 Démarrage Rapide : Installation & Lancement

Suivez ces instructions détaillées pour configurer et lancer l'application sur votre environnement local.

### Prérequis Indispensables

Avant de commencer, assurez-vous que les éléments suivants sont installés sur votre système :

* **Node.js** (version 14 ou supérieure)
* **npm** (Node Package Manager)
* **Python** (version 3.8 ou supérieure)
* **pip** (Python Package Installer)

### Instructions d'Installation

1.  **Clonez le dépôt GitHub** :
    ```bash
    git clone [https://github.com/Greenf1/Intelligent-Surveillance-System.git](https://github.com/Greenf1/Intelligent-Surveillance-System.git)
    cd Intelligent-Surveillance-System
    ```

2.  **Installez les dépendances du Frontend** :
    ```bash
    cd client
    npm install
    cd ..
    ```

3.  **Installez les dépendances du Backend** :
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # Pour Windows : venv\Scripts\activate
    pip install -r requirements.txt
    ```
    > **Note** : Si le fichier `requirements.txt` n'est pas présent, vous pouvez le générer après avoir activé votre environnement virtuel et installé les packages nécessaires en exécutant : `pip freeze > requirements.txt`.

### Lancement de l'Application

Une fois les dépendances installées, vous pouvez démarrer les deux parties de l'application :

1.  **Démarrez le Backend (Serveur Flask)** :
    ```bash
    source venv/bin/activate  # Pour Windows : venv\Scripts\activate
    python src/main.py
    ```
    Le serveur sera accessible localement à l'adresse : `http://localhost:5000`.

2.  **Démarrez le Frontend (Serveur de Développement React)** :
    ```bash
    cd client
    npm start
    ```
    Le frontend démarrera généralement sur `http://localhost:3000` et ouvrira automatiquement l'application dans votre navigateur par défaut.

---

## 💡 Guide d'Utilisation

### Accès au Système

Naviguez vers l'URL de l'application (localement : `http://localhost:3000`).

Utilisez les **identifiants par défaut** pour vous connecter :
* **Nom d'utilisateur** : `admin`
* **Mot de passe** : `admin123`

### Exploration du Tableau de Bord

Après une connexion réussie, le **tableau de bord intuitif** vous attend. Il vous permettra de visualiser et de gérer efficacement toutes les données de surveillance : zones, alertes et métriques système.

### 🌐 API Backend : Points d'Accès

Le backend expose une série de points d'API RESTful pour une interaction programmatique et transparente :

* `POST /api/auth/login` : Authentification des utilisateurs.
* `POST /api/auth/logout` : Déconnexion sécurisée de l'utilisateur.
* `GET /api/auth/user` : Récupération des informations de l'utilisateur actuellement connecté.
* `GET /api/zones` : Récupère la liste de toutes les zones de surveillance.
* `POST /api/zones` : Crée une nouvelle zone de surveillance.
* `PUT /api/zones/<int:zone_id>` : Met à jour les détails d'une zone spécifique par son ID.
* `DELETE /api/zones/<int:zone_id>` : Supprime une zone spécifique par son ID.
* `GET /api/alerts` : Récupère toutes les alertes générées par le système.
* `PUT /api/alerts/<int:alert_id>/resolve` : Marque une alerte spécifique comme résolue.
* `GET /api/metrics` : Récupère les métriques globales et détaillées du système.
* `GET /api/threat-levels` : Récupère les niveaux de menace agrégés par zone.
* `GET /api/status` : Vérifie le statut des différents services intégrés (ex: OpenAI, base de données).

Le frontend est entièrement architecturé pour communiquer et interagir de manière transparente avec ces APIs, assurant une expérience utilisateur fluide et une gestion des données automatisée.
Here are some ideas to get you started:
